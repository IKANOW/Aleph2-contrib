language: java

jdk:
 - oraclejdk8

before_install:
 - cd core_dependencies && git pull origin master && cd aleph2_data_model && mvn install -DskipTests=true && cd ../..

env:
 - TEST_DIR=aleph2_crud_service_mongodb

script: cd $TEST_DIR && mvn install -DskipTests=true && mvn test

after_success:
 - mvn clean test jacoco:report coveralls:report

